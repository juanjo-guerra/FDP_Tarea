#include <iostream>
using namespace std;

int main(){
    /*
    
    
    int intentos = 3;
    string user, password, usuario="tobi_38", contrasena="3006";

    cout<< "Bienvenido, ingrese su usuario" <<endl;
    cin>>user;

    cout<<"Ahora su contraseña"<<endl;
    cin>>password;

    if (user == usuario && password == contrasena) {
        cout << "Bienvenido, " << usuario << endl;
    } 
    else {
        cout << "Incorrecto." << endl;

        
        cout << "Intento 2 - Ingrese usuario: "; 
        cin >> user;

        cout << "Intento 2 - Ingrese contraseña: ";
        cin >> password;

        if (user == usuario && password == contrasena) {
            cout << "Bienvenido, " << usuario << endl;
        } 
        else {
            cout << "Incorrecto." << endl;

            
            cout << "Intento 3 - Ingrese usuario: "; 
            cin >> user;

            cout << "Intento 3 - Ingrese contraseña: "; 
            cin >> password;

            if (user == usuario && password == contrasena) {
                cout << "Bienvenido, " << usuario << endl;
            } 
            else {
                cout << "Acceso bloqueado tras 3 intentos." << endl;
            }
        }
    }

    return 0;
}
    No se puede realizar de manera optima, necesita el uso de bucles.
    */


